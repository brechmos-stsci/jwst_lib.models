allOf:
- $ref: core.schema.yaml
- $ref: referencefile.schema.yaml
- type: object
  properties:
    data:
      title: The pixel area array
      fits_hdu: SCI
      default: 1.0
      datatype: float32
      max_ndim: 2
    meta:
      type: object
      properties:
        pixel_area_sr:
          title: Nominal pixel area in steradians
          type: number
          fits_keyword: PIXAR_SR
        pixel_area_a2:
          title: Nominal pixel area in square arcsec
          type: number
          fits_keyword: PIXAR_A2
$schema: http://stsci.edu/schemas/fits-schema/fits-schema
